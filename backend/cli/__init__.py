"""Cogent CLI — command-line interface for managing the AI coworker.

Analogous to Hermes' ``hermes_cli/`` package — provides management
commands for server, status, tools, credentials, and scheduler.

Usage::

    python -m backend.cli --help
    python -m backend.cli server start
    python -m backend.cli status
    python -m backend.cli tools list
    python -m backend.cli auth list
    python -m backend.cli cron list
    python -m backend.cli kanban list
"""
