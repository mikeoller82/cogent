# Dev dependencies — not for production or container images.
# Install with: pip install -r requirements-dev.txt (includes runtime)

-r requirements.txt

pytest-asyncio>=0.21.0
pytest>=8.0.0
black>=24.1.1
isort>=5.13.2
flake8>=7.0.0
mypy>=1.8.0
